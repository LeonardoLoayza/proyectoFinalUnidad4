from django.contrib import admin
from .models import Project # project is the model we created in portfolio

admin.site.register(Project) # now we register it inside admin site, and we can create new projects. Just like we modeled it 
























